{
    "name": "rigshow-ar",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "start": "uvicorn src.app:app --reload --port=8000",
      "set-reqs": "pip freeze > requirements.txt"
    }
  }